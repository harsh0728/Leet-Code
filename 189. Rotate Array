class Solution {
public:
    void rotate(vector<int>& nums, int k) {
        // 1st Linked list
        // todo next 

        // 2nd reverse array and reverse upto 1st k indexes and remaining
        // Time Complexity: O(n)
        // Space Complexity: O(1)
        k = k % nums.size();
        if (k==0 || nums.size()==1) return;
        reverse(nums.begin(),nums.end());
        reverse(nums.begin(),nums.begin()+k);
        reverse(nums.begin()+k,nums.end());

    }
};
// 99 3 100 1
// 3 99 1 100

// 7 6 5 4 3 2 1
// 5 6 7 1 2 3 4
